{
  "name": "emails-input-example-vanilla-npm",
  "version": "1.0.0",
  "description": "EmailsInput example in vanilla typescript",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "npm run clean && parcel build index.html",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@marcin-pajak/emails-input": "^0.1.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "typescript": "^4.0.3"
  },
  "keywords": [
    "typescript",
    "javascript"
  ]
}
